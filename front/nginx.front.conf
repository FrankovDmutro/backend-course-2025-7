server {
    listen 80;
    server_name localhost;

    access_log /dev/stdout combined;
    error_log /dev/stderr info;

    root /usr/share/nginx/html;
    index RegisterForm.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
