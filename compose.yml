services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-course-2025-7
    env_file:
      - ./app/.env
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0
    restart: unless-stopped